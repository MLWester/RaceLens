const TrackMap = ({ data, selectedLaps, width = 800, height = 400, showSectors = false, highlightSector = null }) => {
  return (
    <div 
      style={{ width, height }} 
      className="bg-gray-100 dark:bg-gray-700 rounded-lg flex items-center justify-center"
    >
      <p className="text-gray-500 dark:text-gray-400">Track visualization coming soon</p>
    </div>
  );
};

export default TrackMap; 